//Tableau 

let TableauNames=[];
TableauNames.push("Vincent");
TableauNames.push("Paul");
TableauNames.push("Arthur");

for (let i=0;i<TableauNames.length;i++) {
    console.log(TableauNames[i] + " va a la peche");
}